import React from "react";
import { action } from "@storybook/addon-actions";

import Menu from "./Menu";

export default {
  title: "Menu 导航菜单",
  component: Menu,
};

// export const defaultMenu = () => {
//   return <Menu onClick={action("default menu")}>default menu</Menu>;
// };
// defaultMenu.story = {
//   name: "defaultMenu1",
// };
